import style from '../style/Spinner.module.scss'

/**
 * Snipper for loading state, renders in the center of the screen
 */
function Spinner() {
  return <div className={style.spinner} />
}

export { Spinner }
